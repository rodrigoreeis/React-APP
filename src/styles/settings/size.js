import { css } from 'styled-components';

const GlobalSize = css`
  :root {
    --spacing--small: 10px;
    --spacing--medium: 20px;
    --spacing--big: 40px;
    --radius: 4px;
  }
`;

export default GlobalSize;
